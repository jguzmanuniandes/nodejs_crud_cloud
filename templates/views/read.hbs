<!DOCTYPE html>

<html>

<head>
    <title>Read</title>
    <link rel="icon" href="#">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/create.css">
</head>

<body>
    <div class="main-content">
        {{>header}}
        <p>Read event</p>
        <form>
            <label for="fname">Name</label>
            <input type="text" id="fname" name="name" value="{{event.name}}" readonly>

            <label for="lplace">Category</label>
            <input type="text" id="lname" name="category" value="{{event.category}}" readonly>

            <label for="lplace">Place</label>
            <input type="text" id="lname" name="place" value="{{event.place}}" readonly>

            <label for="laddress">Address</label>
            <input type="text" id="lname" name="address" value="{{event.address}}" readonly>

            <label for="lstartdate">Start Date</label>
            <input type="text" id="lname" name="startDate" value="{{event.startDate}}" readonly>

            <label for="lenddate">End Date</label>
            <input type="text" id="lname" name="endDate" value="{{event.endDate}}" readonly>

            <label for="lisvirtual">Is virtual?</label>
            <input type="text" id="lisvirtual" name="isVirtual" value="{{event.isVirtual}}" readonly>
        </form>
    </div>

    {{>footer}}
</body>

</html>